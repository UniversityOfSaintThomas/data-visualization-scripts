1. could you clarify a bit better how report patrick has updates the JSON? Would it be so that whenever I see an update pushed to the repo that changes the contents of that JSON file, that is when I run the pipeline to execute codebuild to run the script to post to cascade?
2. I'll be doing this at first without any API Keys, but who should I reach out in order to get those keys that I need to be generated? Or do we already have the API keys so that I can start testing from the start?
3.
